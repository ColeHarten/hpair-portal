import {Button, Box, Typography} from "@mui/material";

export default function Home({user}){
  return (
    // set up home screen
    <Box>
      <Typography variant="h1">Welcome to the HPAIR Portal!</Typography>
    </Box>
  );
}
