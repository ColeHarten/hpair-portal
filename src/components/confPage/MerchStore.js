// Function to hold the mech store page
import { Box, AppBar, Toolbar, Button, Typography } from '@mui/material';
import React from 'react';

export default function MerchStore({user}) {
    return (
        <Box>
            <Typography> Merch Store </Typography>
        </Box>
    )
}